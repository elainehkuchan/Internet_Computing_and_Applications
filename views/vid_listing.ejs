<html lang="en">

   <%- include header %>
   <!-- Main component for a primary marketing message or call to action -->
      <div class="jumbotron">
        <h1>Welcome</h1>
        <p>Video Listing</p>
        <p>
          <ul>
              <% videos.forEach(function(video) { %>
                  <li><%= video.name %> - <%= video.userid %></li>
              <% }); %>
          </ul>
        </p>
      </div>
    <%- include footer %>